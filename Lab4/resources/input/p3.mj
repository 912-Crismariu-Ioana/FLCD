# Compute the sum of n numbers

BEGIN
my $n:int, $sum:int, $term:int, $ctr:int;
stdin($n);
$sum = 0;
$ctr = 0;
while ($ctr < $n) do {
    stdin($term);
    $sum = $sum + $term;
    $ctr = $ctr + 1;
};
print($sum);
END